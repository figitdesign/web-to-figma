{
  "name": "@figit/oracle-harness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Visual-parity harness: measure DOM-to-Figma correspondence and drive autonomous fixes.",
  "scripts": {
    "cli": "tsx src/cli.ts",
    "parity": "tsx src/cli.ts snapshot --run-id parity && tsx src/cli.ts report --run-id parity && tsx src/cli.ts check --run-id parity",
    "check-types": "tsc --noEmit",
    "lint": "biome check",
    "test": "vitest run --passWithNoTests",
    "test:browser": "ORACLE_BROWSER=1 vitest run --passWithNoTests"
  },
  "dependencies": {
    "@figit/dom-to-figma": "workspace:*",
    "@figit/fig-kiwi": "workspace:*",
    "playwright": "^1.50.0",
    "tsdown": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.19.2",
    "typescript": "catalog:",
    "vitest": "^4.1.5"
  }
}
